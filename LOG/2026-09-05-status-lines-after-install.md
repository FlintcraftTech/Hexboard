# [HASH] — [status-lines-after-install] cleared, covering a third stale sentence the item had not named

Two sentences went false when the install succeeded on 2026-09-03: `CLAUDE.md`'s phase paragraph saying there is no input method service, and `README.md`'s Status section saying there is no working keyboard and nothing to install. A third was found while processing it — README's "Try the prototype" section still calls the browser prototype the only part of Hexboard you can type on. Fixing two while a third contradicted them would have left the document arguing with itself.

One distinction is kept rather than smoothed over: "nothing to install" is false in one sense and true in another, since the project builds from source and runs on a phone while no packaged release exists. A Status section claiming a working keyboard without that would send a reader looking for a download that is not there, so it says both.

The phase paragraph's closing instruction — "Very little exists, so keep designing before coding" — rests on a premise that has gone, and the user's call was to keep its force and reword it as intent. Dropping a working rule for a wording reason would have been the wrong trade.

**Queue changes:** [status-lines-after-install] rewritten and moved into Processed, cleared to run.

**Work processed:** kept — [status-lines-after-install].
