# 73231d1 — Planning session of 2026-09-02, chat-level record: a layout editor designed and deleted in one day, dictation researched into three items, and SPEC's principles measured

Fourteen items were given a disposition and each carries its own record under its slug; this entry holds what belongs to the chat rather than to any one of them.

The session's shape is worth naming because the record would otherwise read as churn. Two full designs were written and then removed the same day — the contributor-facing layout editor and phone-side layout creation — because the user reconsidered the premise underneath them rather than the designs themselves. Both removals were their explicit direction. SPEC's platform principle was consequently rewritten twice in one session, first to describe an editor and contributed layouts and then to describe layouts copied from each language's own standard. The second rewrite replaced sentences written the day before, which is recorded on [spec-coherence-readthrough] as a further reason for that read.

A correction Claude made to its own work, caught by /rescan rather than at the time: the walkthrough written for [russian-layout-check] opened by asking the user to edit a JavaScript block in the preview page, which Claude can do. `[user]` is reserved for work Claude genuinely cannot perform, and preparing the preview moved into [language-starter-layouts]. The over-tag was introduced and corrected within the same session.

Two research findings were filed with their index lines and frame assessments: `cyrillic-overflow-and-slot-budget.md` and `gboard-speech-correction.md`. Both record what their sources actually were — English-language explainers of the Russian layout in the first, six-year-old architecture papers in the second — rather than presenting either as settled current fact.

A method report was considered and deliberately not filed. The queue lint's resolved-flag advisory fired on nearly every tool call for the rest of the session after a `Blocked by:` line was removed, still naming an item that had since been deleted and a line number it no longer occupied. It behaves as built, diffing the working tree against the last commit, and the repetition follows from an unusually long uncommitted session. Claude recommended dropping it rather than filing something thin, and the user agreed. Recorded so the decision not to report is visible rather than looking like an oversight.

The forward advisory left by the previous session, which pointed at [repo-go-public], was surfaced at the opening and cleared as spent.

**Also in this chat:** a script writing to a runtime-computed path was refused by the safety check and rewritten as explicit `Write` calls, which is the guard working as intended rather than a fault. The queue lint also caught a missing rule-gate disposition on [claude-md-phase-stale]; the gate was read and a disposition written, without which the build would have halted. The session ran across a restart of the chat's own tooling, with the same plugin version and build stamp on both sides.

**Queue changes:** eight items filed, two filed and deleted the same day, one deleted from Processed, five rewritten or narrowed; the cleared region grew from four items to eight; SPEC's platform and key-inventory principles rewritten; two research findings and their index lines added.

**Work processed:** kept and cleared to run — [variant-language-fields], [language-starter-layouts], [spec-principles-rework], [claude-md-phase-stale]. Kept and held — [layout-switching], [russian-layout-check], [language-list-choice], [ondevice-recogniser-test], [recogniser-gap-comparison], [speech-output-correction], [spec-coherence-readthrough]. Deleted — [variant-editor], [in-app-layout-creation].

Advisory: filed — repo-go-public
