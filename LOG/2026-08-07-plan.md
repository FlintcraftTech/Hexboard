# [HASH] — Four unprocessed items cleared: `hexboard-plan.md` folded into SPEC and deleted, [ignore-idea-folder] folded into [untrack-faq], two design threads sharpened and deferred, and three SPEC corrections made

The session's shape was set by the previous close's advice: process [ignore-idea-folder] first so the `.gitignore` that [untrack-faq] creates is written complete in one pass rather than created and then reopened. That fold went through. The separate-motivation reason the item had been filed alone is about why the line is wanted, not about what the build does, and motivation doesn't earn its own build item — the reason survives in [untrack-faq]'s prose, so nothing was lost by merging them.

`hexboard-plan.md` was read in full against SPEC before being deleted. Almost all of it was genuinely superseded, and two of its three open questions had closed since it was written: glyph sizing was settled at 0.92×, and hit-test verification is queued as [verify-a11y-ondevice]. Three things weren't recorded anywhere else. The five-emoji-panel count and the falsifiability of the perceptual claim went into SPEC. The left-space relocation question — move left space from row 3 col 4 to col 2 for thumb symmetry, then decide where the displaced comma goes and what fills the vacated slot — became capture [left-space-relocation], because it is undecided work rather than product truth. It was confirmed live against `resources/key-layout.json` rather than assumed: left space is still at col 4 and the comma still at col 2. The option of keeping the file with a "superseded" banner at the top was weighed and lost, because it leaves two overlapping descriptions of the project in a repo that is about to be public, which is the problem the item was filed to solve.

On the falsifiability line: SPEC said the differentiator is settled, which is true about not re-arguing it, but read alone it could be taken as the claim having been proven. It hasn't been tested. The added sentence keeps the settled-ness while saying plainly that the claim predicts something measurable and would not survive research showing the effect absent — a more honest thing for a public visitor to find, and consistent with the plan doc it replaced, which said the same.

Both remaining items were sharpened and deferred rather than kept or dropped, since neither can yet be described as a build. [variant-editor]'s stated prerequisite has quietly cleared — the key config now exists — but the three things actually blocking its design are unchanged and aren't resolvable by more planning: no keyboard, so nothing to vary; no fork, so the upstream-sync question has no concrete case; and the scope decision underneath it is better made with a working keyboard in hand. [uniform-neighbours-predictive] gained a fourth caveat that would otherwise have cost a later session: neighbour sets are not derivable from the config alone. The config carries row and column only, because geometry is deliberately excluded from it, so which six keys neighbour a given key depends on the zag parity rule that lives in Kotlin. The neighbour table is computed at runtime from both. SPEC asserted the config-only version, so the spec-sync gate fired and that sentence was corrected in the same close.

A gap surfaced at the wind-down and was captured rather than resolved: `resources/key-layout.json` holds no emoji panels, though SPEC says five exist and both SPEC and the config's own description call it the single source of truth for which characters exist. Either emoji belong in it, or both documents need a sentence scoping the manifest rules to the non-emoji panels.

**Queue changes:** `advisory-3` consumed and cleared. Two captures filed — [left-space-relocation] and [emoji-panels-missing-from-config]. [untrack-faq] widened to write three paths and verify five things. [variant-editor] and [uniform-neighbours-predictive] each sharpened and moved to the bottom of Unprocessed. SPEC.md edited three times: five emoji panels, the claim is settled but testable, and the corrected neighbour-derivation sentence. `hexboard-plan.md` deleted via `git rm`.

**Work processed:** deleted — [ignore-idea-folder] (content relocated into [untrack-faq]), [stale-plan-doc] (content relocated into SPEC and [left-space-relocation]). Skipped to a later session — [variant-editor], [uniform-neighbours-predictive]. Kept — none.
