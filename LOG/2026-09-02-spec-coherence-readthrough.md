# [HASH] — The SPEC read-through narrowed from four questions to the one the user alone can answer

The item asked the user to read SPEC whole and report four things: whether it still describes one product with a point of view, which sentences read as machinery rather than product truth, what says more than its importance warrants, and then a report.

Two of those four should never have been the user's. Whether a sentence names internal fields or file formats, and whether a minor feature has outgrown the perceptual wedge, need a careful read and no judgment about what the user meant — and the standing rule reserves `[user]` for work Claude genuinely cannot do. Claude ran both passes in this session under the resolve-now rule, and they became [spec-principles-rework].

What is left is the one thing that is irreducibly the user's: whether SPEC still sounds like *their* project. Claude wrote most of those sentences, so it would be marking its own homework. The walkthrough is now two steps.

The item's premise also strengthened during the session rather than weakening. It was filed because SPEC took seven changes in one day; today the platform principle was rewritten again, over sentences written the day before, and the key-inventory principle reworded to match. That is recorded on the item as a further reason to read the whole thing.

It is now held against [spec-principles-rework], so the coherence read happens once, after the rebalancing, rather than twice.

**Queue changes:** narrowed from four walkthrough steps to two; `Blocked by: [spec-principles-rework]` added; moved to the end of the held region with the other `[user]` stops.

**Work processed:** kept, held below the line — [spec-coherence-readthrough].
