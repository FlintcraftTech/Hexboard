# 770b778 — [spec-out-of-scope-stale] SPEC's out-of-scope line deleted rather than rewritten, everything true in it already being stated elsewhere

Session of 2026-09-12, 12:54.

SPEC's last line deferred "IME service polish (settings screen, language switching)" for early iterations, and the layout picker built on 2026-09-05 is both of those — so the line deferred work that had shipped. Carried forward from [layout-switching], which noticed it while being designed and would have taken the observation with it when it shipped.

The entry suspected the replacement would need a read of SPEC end to end to say what is genuinely still deferred. That read happened at this session's opening, and the answer is that SPEC marks its deferrals where they belong rather than in a list at the bottom: voice adaptation says it waits on voice input, predictive text records that its own condition has been met, and the genuine out-of-scope statement — languages whose writing is not selected from a key inventory — sits in the layouts principle where a reader meets it in context. The line above it already says theming and customisation come later.

So the line had no work left to do, and a rewritten version would have been a heading looking for content.

**Queue changes:** SPEC's out-of-scope line removed; [spec-out-of-scope-stale] deleted.

**Work processed:** deleted — [spec-out-of-scope-stale].
