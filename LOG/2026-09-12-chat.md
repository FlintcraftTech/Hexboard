# 770b778 — Planning session of 2026-09-09 to 2026-09-12, chat-level record: ten items lifted at the opening, twenty-one given a disposition, eleven SPEC sentences, and three method reports sent

This session ran across 2026-09-09 and 2026-09-12. Session of 2026-09-12, 12:54.

**It opened on a queue with nothing to build.** Thirteen items sat below the readiness line and none above it, and the session-start facts said no blocker was still in Unprocessed — which is the signature of work held against things that have since shipped. Ten of the thirteen lifted at the opening: the build run ending 2026-09-09 had shipped the neighbour table, the word list, the landscape height bound and the dictation prompter, and had driven [verify-this-runs-build-on-device] to its end, which was the blocker five items shared. Each lifted item now says in its own text what cleared it. Three stayed held behind work still in the queue.

**The throughput floor was one item** — no blockers in Unprocessed, plus one because nothing was cleared to run — and the lifts alone met it. Twenty-one entries were then given a disposition anyway, and the queue finished with twenty items cleared to run and nine held.

**The ordering ladder fell through four of its five rungs over the session**, which is worth recording because it is the first time this project has seen the bottom one: due cycle work first, then unblock-potential, then the long-and-old rung, then the alternating decay rung. Two entries filed mid-session — [play-store-release] and the split-out [word-list-ok-by-name] — made rung 3 live again after the session had already fallen past it.

**Eleven SPEC sentences were written**, seven during the session and four at the close's spec-sync gate, which found decisions made this session whose product truth nobody had written: the per-language quotation marks, the five structural keys every layout carries, Hexboard announcing itself as one keyboard rather than one per language, and dictation leaning on saved words. The gate reads the session's own diffs rather than anyone's memory, which is what caught all four.

**Also in this chat:**

- **Claude put a settled design to the user as an open question, and he caught it in one line.** Presenting [personal-voice-model], Claude asked whether Hexboard would carry a speech model of its own; the answer had been settled on 2026-09-01 and is written in the item, in SPEC and in the cited research. His words: *"I thought we already designed this and found small open source models that can do it."* He then asked for a report on how it happens, which produced the first of two method reports this session. The theory in it: the session ran the queue digest's next-pick command through a shell pipe that truncated its output, and reasoned over the fragment — a truncation no check can see, because the tool returned everything it was asked for. Two contributing mechanisms named alongside it: the prior-decisions retrieve does not fire on a question the session believes it derived rather than retrieved, and the ladder's cheapest rung tells a session it "was there" for decisions a context restart has taken out of view.
- **A second method report went out at the rescan**, on `Blocked by:` resolving as soon as the named entry is kept into Processed — so a capture waiting on work being *built* is released at the earliest possible moment and offered every session afterwards. Two instances from this session, and the asymmetry with the held-work revisit's built-and-verified test, are in it. Both reports are in `INBOX/sent.md`.
- **A third method report went out from the close itself.** The commit step showed the commit message and then announced the commit, with no ask, and narrated that no push was being offered. The user said plainly that he could not tell what was wanted and that the ask was never made. The report carries both: a genuine tension between the always-loaded write-first table, which puts a commit message in show-then-wait, and done.md's "already approved, so nothing new to confirm" — true of a build close, where the entry was approved, and not of a planning one, whose entry is written under write-first; and the smaller wording fault of narrating a withheld default at the user.
- **Four times, a message asserted when something happened with no source** — "today", "earlier today", "a few minutes ago", "an hour ago" — and was stopped each time. The clock or the record was read and the sentence rewritten. Worth recording as a pattern rather than four slips: relative time words are the ones that reach for memory hardest, and the record is where a wrong one would have stuck.
- **The emoji-refresh cycle ran its first turn and found nothing**, which is a completed turn: Unicode's listing still tops out at 16.0 and the bundled file is 16.0. Recorded under its own slug, which is what gives the cycle's observable something to read.
- **One deletion was offered, agreed, and then deliberately left for a build run.** The user approved deleting the stale `android/app/build/` folder and, asked whether to do it in the planning session through the scope-lock's door, chose to leave it queued instead.

**Queue changes:** ten items lifted off the held region at the opening; twenty-one entries given a disposition; six deleted; four filed this session ([play-store-release], [word-list-ok-by-name], [spanish-letter-panel-gaps], [judge-drawing-values-on-phone]) plus [claude-md-swipe-wording]; `[user]` and `[freeform]` lines batched at the ends of the cleared region at the close.

**Work processed:** twenty-one entries — kept fifteen, deleted six. Each carries its own entry under its slug.

**Advisory: not needed** — nothing in Unprocessed is available for the next planning session to process. All seven remaining entries are dated out, bowed out behind an open entry, or standing as a reminder of a decision that is the owner's to make.
