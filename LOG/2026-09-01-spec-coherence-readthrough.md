# be3516e — A SPEC read-through filed as user work, because seven changes in one session were never read end to end

Filed by /rescan from a suggestion made in passing during the same session's planning and never written down — which is exactly the class of thing a rescan exists to catch.

SPEC took seven changes on 2026-09-01: the default-layout rewording, the variant-file rule written at the close, where the layout picker lives, press feedback, the persistent clipboard, voice input, voice adaptation, and the predictive text rewrite. The stale "Project docs" section went the same day. Each sentence was written against its own discussion and is correct on its own terms; nobody read the result whole.

The risk is a document accurate line by line that no longer reads as a description of a keyboard — a pile of rulings rather than product truth. That is a judgment about tone and coherence, which is why it is the user's rather than Claude's, and why it is `[user]` work rather than an audit.

Its walkthrough asks for one uninterrupted read without fixing anything, then three specific things to look for: whether it still describes one product with a point of view, anything that reads as implementation machinery rather than product truth, and anywhere a minor feature has outgrown the perceptual wedge in the Principles list. Any rewording that follows is ordinary planning work filed from the report.

The item names no observable, and says so: nothing in the world changes when it is done, so it waits until the user mentions it rather than being checked.

**Queue changes:** [spec-coherence-readthrough] filed in Unprocessed as a `[user]` item.

**Work processed:** filed as a capture — [spec-coherence-readthrough]. Not routed; that is the next planning run's.
