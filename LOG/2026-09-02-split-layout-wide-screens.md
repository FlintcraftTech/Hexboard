# 6f66348 — [split-layout-wide-screens] settled as automatic whenever width exceeds height, written into SPEC, and held in Unprocessed until the keyboard can be seen in landscape

Planning record for [split-layout-wide-screens], 2026-09-02.

The user's idea from the morning, filed right after keeping the 4-and-6 space bars: on a wide screen the thumbs move to the edges, so split the board into two halves with the gap in the middle. Of the open questions, one was decidable at a desk — automatic or a setting — and the user answered it: the split is automatic, and the trigger is the screen being wider than it is tall. Not a device class: a phone in landscape gets it, a tablet in portrait does not. Gboard's split-as-a-toggle was named as the comparison and not chosen. That sentence is in SPEC.

What remains — the gap width and what each half does with the space bars — is seen rather than reasoned, and wants the keyboard running as a real input method in landscape, which [install-and-enable-on-pixel] delivers. So the capture stays in Unprocessed with that item as its blocker and returns by itself; the likely space-bar answer (each half keeps the one nearer its thumb) is recorded as the starting point. The ordering is written on both items.
