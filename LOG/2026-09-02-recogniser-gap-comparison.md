# [HASH] — The dictation comparison split off as user work, with what each possible result would mean written into it

Split from [ondevice-recogniser-test] because only the user has the phone and only a person can speak. Beyond that, the two established impossibilities stand: there is no `adb` on this machine and Gradle cannot run here, both by attempt rather than assumption and both recorded on earlier items.

The walkthrough asks for three passages — ordinary sentences, one carrying names and places, and one spoken at natural rather than dictation speed — dictated through Gboard and through Hexboard's test screen, with the availability line reported first.

What makes it worth doing rather than merely doable is written into the item: near-identical output suggests the same engine behind both doors, leaving punctuation and capitalisation as the work, while a clear Gboard advantage means the gap sits inside recognition where correcting a finished transcript cannot reach. Without that, a report of two transcripts is data nobody knows how to read.

The research behind it is in the [speech-output-correction] entry of the same date.

**Queue changes:** filed into Unprocessed and placed at the end of the held region with `Blocked by: [ondevice-recogniser-test]`, keeping the `[user]` stops batched together.

**Work processed:** kept, held below the line — [recogniser-gap-comparison].
