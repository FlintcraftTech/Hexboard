# 1979343 — The chat around the run: an item halted on bad arithmetic, an ordering risk accepted, and five captures filed

The chat-level record for the /next run of 2026-09-02, covering what belongs to no single work item. Each built item and each `[user]` item has its own entry of the same date.

**The run was started on an ordering the previous session had advised against, and the user accepted that knowingly.** The advisory said [repo-go-public] sat first while two cleared items behind it — [spec-principles-rework] and [claude-md-phase-stale] — correct text that publishing would expose. That risk was named before anything ran and the user chose to proceed. It turned out to be moot: the repository was already public, so the exposure had already happened, and both corrections landed in the same run.

**[language-starter-layouts] was halted rather than built, on a premise that did not survive contact with the config.** The item states that the QWERTY panel gives 30 letter slots, so Russian's 32 letters fit once two move behind a long-press. Rows 0–2 do hold 30 slots, but four are occupied by backspace, cursor-left, cursor-right and enter, so the panel offers 26 letter positions and English fills all of them exactly. Russian would need six long-press pairings rather than two — and the two the item already proposes are Claude's own reading, awaiting a Russian reader's check in [russian-layout-check], so inventing four more compounds an unverified judgment. The overflow rule is also written to bind every later language, which makes this product truth rather than a build detail. Filed as [qwerty-letter-slot-count]; the item stays in Processed, unedited, with its file list and observation still assuming the wrong figure. The user was told and chose to carry on with the two remaining build items.

**Two build-config faults were found by driving a `[user]` item and fixed with the user's agreement, each added to the run's file list before it was edited.** Both are recorded in full in the [run-key-config-validator] entry. Neither belonged to any queue item: they were discovered because this was the first session in which anything was actually compiled.

**A correction to a walkthrough's own figure.** [run-key-config-validator] says "all six tests green"; the test file now carries seven `@Test` methods and seven passed. The walkthrough's number was stale, not the result.

**Five captures were filed across the session**, three of them by a /rescan run shortly before this close: [qwerty-letter-slot-count], [backspace-key-repeat] and [split-layout-wide-screens] during the run, then [assets-srcdir-deprecation], [install-walkthrough-refresh] and [android-studio-step-authoring] at the rescan, and [claude-md-phase-ran] at this close — seven in total. Two of them are about how this method's own walkthroughs are written rather than about the keyboard.

**Also in this chat:** the user asked which of two Android SDKs to point the project at, and chose Android Studio's own on the recommendation that it is the one Studio's SDK Manager keeps current; both carried the platform the module needs. A "Save Context" dialog opened by an accidental shortcut was identified and dismissed. The `scripts/__pycache__/` folder was created by a throwaway import used to exercise the new config validation.

**Files touched:** none beyond those recorded on the individual entries.

**Routed to Captures:** [qwerty-letter-slot-count], [claude-md-phase-ran] — the others are recorded on the entries whose work produced them.

Advisory: filed — qwerty-letter-slot-count
