# [HASH] — Direct-boot held behind two builds using the `until built` suffix, which is what the entry had wanted and could not express

Recorded 2026-09-17 13:59.

Hexboard cannot start until the first unlock after a reboot, so the unlock itself is typed on whatever keyboard the system falls back to. Half of this entry closed on 2026-09-12 when SPEC gained the sentence stating the behaviour; what remained was the design, and it is not designable — the question is what Hexboard must read before unlock, and two of its three answers belong to features nobody has built.

The entry already named its own trigger in prose — [persistent-clipboard] and [predictive-saved-words] being built — and explained why it was prose rather than a field: a `Blocked by:` line resolved the moment an entry was kept into Processed, and both already were, so it would have recorded the ordering without holding anything back.

That reasoning has lapsed. The line now takes an `until built` suffix, which holds a capture until every named entry has a build record — exactly the trigger the paragraph described. So the hold the entry wanted and could not write is now writable, and the entry stops being offered every session.

It is a chain, [predictive-saved-words] being itself held behind [uniform-neighbours-predictive], but a terminating one: that engine is cleared to run with nothing in front of it.

The argument for eventually supporting direct boot rather than doing nothing is untouched and worth keeping in view: someone who chose Hexboard because larger targets make them accurate is denied that at exactly the moment a mistyped unlock costs most, and SPEC treats accessibility as a requirement rather than a nicety.

**Queue changes:** `Blocked by: [persistent-clipboard], [predictive-saved-words] until built` written on [direct-boot-unavailable], with the superseded prose-not-field reasoning recorded in place; the ordering written onto both named entries.

**Work processed:** kept as a held capture — [direct-boot-unavailable].
