# [HASH] — [studio-closed-before-wrap] relocated into [nested-wrap] and deleted, the step becoming a check the wrap session runs rather than an instruction it can forget

Session of 2026-09-18, 15:42.

The capture said that [nested-wrap] tells a future session to reopen Android Studio at `hexboard/` after the wrap lands, and never says Studio must be shut before the move begins — which it must, because on Windows a move of a file another program holds open fails part-way, and `android/` is exactly what Studio holds. It was filed on 2026-09-18 by the session that opened [nested-wrap] by hand, checked the machine before moving anything, and found Studio running.

**Its own closing paragraph named its answer, and processing it is what that filing was for.** The capture said the sentence belongs inside [nested-wrap] rather than beside it, because the session that needs the warning is the one running that item; a separate capture warns only a session that happens to read the queue first. It also recorded that the amendment had been offered at the close of 2026-09-18 and the capture chosen instead. That was not a rejection of the amendment but a deferral of it: amending a queue entry's wording is planning work by the method's own rule and can never clear as a build, so this entry had no other route out — either the sentence went into [nested-wrap] at a planning session, or the capture sat in Unprocessed being re-offered indefinitely.

**The step written is better than the step the capture asked for, and the difference is the CLI-tool check firing.** "Remember to close Android Studio" is an instruction a session can believe was followed. Running `tasklist` and looking for `studio64.exe` is a condition the wrap session verifies before its first move. The check was run at 14:19 on 2026-09-18 and returned `studio64.exe`, process 19400 — so it both works on this machine and fires as things currently stand, which is the evidence that made the recommendation worth making rather than a plausible suggestion.

One wording point settled it into the right shape: [nested-wrap] is `[freeform]` rather than `[user]`, so it carries prose instructions rather than a numbered walkthrough. The step therefore landed as a paragraph ahead of the file-by-file split, not as an item in a list.

The removal was a relocation rather than a judgment that the work was not worth doing, so the content was carried across first and the entry removed after. [nested-wrap] still names the capture's slug, deliberately: that sentence records which capture the step came from, which is provenance rather than a reference to live work.

**Queue changes:** [studio-closed-before-wrap] deleted from Unprocessed after its content was written into [nested-wrap], which gained one paragraph ahead of its file-by-file split.

**Work processed:** deleted — [studio-closed-before-wrap].
