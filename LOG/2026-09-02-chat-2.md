# PLACEHOLDER — Build run of 2026-09-02, chat-level record: eleven cleared items built in one run and the SPEC read-through walked to its end, with nine builds waiting on the phone to confirm them

Closed 2026-09-02 16:18. The run took the whole cleared region — eleven builds and one user step — after the second planning session of the day had lifted six items on verified blockers. Nothing compiles on this machine, so every Kotlin and Gradle change is ticked unconfirmed and waits on the next Android Studio run; the Russian layout and the two CLAUDE.md sentences were confirmed here. Each item has its own entry; this record holds what belongs to the chat rather than to any item.

**Unconfirmed at this close, named as the close requires:** [first-installable-build] (a Gradle build, then the install walkthrough), [key-press-feedback], [backspace-key-repeat], [long-press-accent-popup], [panel-switch-gestures] and [ondevice-recogniser-test] (all: the app run on the Pixel 6), [android-key-audit] (the test run from Android Studio), [assets-srcdir-deprecation] (the next Gradle sync). Confirmed: [language-starter-layouts], [android-studio-step-authoring], [claude-md-phase-ran].

**The one full-depth decision of the run** is on [panel-switch-gestures]: of the two gesture remedies the research named, the non-consuming detector was already in place from the press-feedback rewrite, so nothing further was added, and the board claims movement only while an accent row is open.

**The user step**, [spec-coherence-readthrough], was walked to its end and is recorded in its own drive record: the user read SPEC and reported it still reads as one product. They added a condition — if Claude's own estimate was that SPEC is not up to standard, message the Throughliner project about SPEC as a construct. Claude's estimate agreed with theirs, so nothing was sent.

**Also in this chat:** the user accepted the twelve-item run as presented with no change of scope or order. A post-run rescan filed six captures, each recorded on the entry of the item that raised it. The twelve uncommitted LOG files at the session's opening were the previous close's hash backfill and ride this commit.

Advisory: filed — [forward-advisory], advising that the install walkthrough be lifted next, since it is what confirms nine of this run's builds; the overlap scan found two of this session's captures bearing on it, and they are named in the advisory.
