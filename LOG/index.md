# LOG Index

One-line summaries of each session. Newest first. Each line names the session's full entry file in this folder. Months that have ended live in `LOG/index-YYYY-MM.md` alongside this file.

- [HASH] — [variant-schema] built: `key-layout.json` gains `id`, `name` and `isDefault` at schemaVersion 2 and its `about` reworded off the retired "single source of truth" wording; `generate-key-manifest.py` taught to read its output path from the config's `generates` and to take `--config`, with the layout named in the manifest header; manifest regenerated, no key data changed; no Kotlin touched because Gson ignores undeclared fields; run ended before [repo-go-public] on its `Runs alone` marker → 2026-09-01-variant-schema-build.md
- be3516e — Planning session of 2026-09-01, chat-level record: [row3-space-choice] lifted after its blocker was confirmed built *and* verified while two siblings stayed held on built-but-uncompiled code; a safety guard on `INBOX/sent.md` fought through four refusals before `touch`-then-Edit worked, recorded as a mistake; two method reports and one article idea sent by mail with the site project added to the address book; the spec-sync gate catching [variant-schema]'s missing SPEC sentence at the close; Processed reordered to batch the held `[user]` items → 2026-09-01-plan.md
- be3516e — [compile-and-view-panel] rewritten from a four-step walkthrough assuming a device target that does not exist into seven steps pairing the Pixel 6 over Wi-Fi first, the compile-only Build → Make Project alternative recorded as rejected for proving nothing about runtime config loading or drawing, the on-screen claim checked against `MainActivity.kt`, and the shared pairing cross-referenced into [install-and-enable-on-pixel]; cleared to run after [run-key-config-validator] → 2026-09-01-compile-and-view-panel.md
- be3516e — [manifest-principle-vs-variants] deleted after the SPEC contradiction it reported was found already fixed in the same 2026-08-20 commit that filed it (`9a85807`, checked); the one surviving word — `key-layout.json` as "the canonical layout" — reworded to "the default layout the app ships with" → 2026-09-01-manifest-principle-vs-variants.md
- be3516e — [variant-schema] created and cleared to run after half of [variant-editor]'s deferral reason proved false: one file per variant, `id`/`name`/fallback fields, and per-layout manifests are desk decisions, while runtime enumeration and pick-storage do wait on a build; the config's retired "single source of truth" `about` text folded in; placed above [repo-go-public] because that item is `Runs alone` → 2026-09-01-variant-schema.md
- be3516e — [variant-editor] given `Blocked by: [variant-schema]` and deliberately kept in Unprocessed rather than below the line, because what the editor is and what it writes are still undescribed even once the schema lands → 2026-09-01-variant-editor.md
- be3516e — [layout-switching] held against [variant-schema], and the picker's home settled in the app's own settings rather than on the keyboard surface, the gesture option losing to the collision with horizontal panel swipe; SPEC's platform principle gained the sentence → 2026-09-01-layout-switching.md
- be3516e — [panel-switch-gestures] designed out as a horizontal pager after `KeyboardPanel.kt`'s board-level tap handler settled the screens-versus-one-surface question; `detectTapGestures` consuming the pointer-down identified as the trap that breaks the obvious construction, with two named remedies, filed as `compose-pager-vs-board-tap-gesture.md`; re-held against [compile-and-view-panel] instead of a vague condition → 2026-09-01-panel-switch-gestures.md
- be3516e — SPEC's scaffold-written "Project docs" section cut entirely rather than replaced by a pointer, nothing being lost since CLAUDE.md's managed block and README.md both carry it more currently; [spec-project-docs-stale] deleted as done → 2026-09-01-spec-project-docs-stale.md
- be3516e — The epoch-5 `resources/` folder-move trap reported by mail to the plugin's own project after verifying the instruction is still live in 1.21.1-test5; scrubbed to describe the general case rather than this project; `INBOX/sent.md` created and [report-epoch5-resources-assumption] deleted → 2026-09-01-report-epoch5-resources-assumption.md
- be3516e — [uniform-neighbours-predictive] gains saved words (reversing SPEC's "stores nothing", with the consent trail and three binding constraints recorded, and the red flag's basis moving from designed-out to informed consent) and a no-proper-nouns rule; the word-list question answered in-session — SCOWL's licence and excludable proper-name categories fit, frequencies do not, Leipzig beats wordfreq on ShareAlike → 2026-09-01-uniform-neighbours-predictive.md
- be3516e — [persistent-clipboard] filed with an hour-or-twenty retention rule, a clipboard screen with drag-to-bin deletion, and deliberately no clear-all because an emptied history is itself a disclosure; Android's `EXTRA_IS_SENSITIVE` established as advisory and source-app-set; a five-minute expiry for password-field clips beating Claude's proposed exclusion → 2026-09-01-persistent-clipboard.md
- be3516e — [key-press-feedback] settled as the pressed key lightening and fading with no touch-point marker, the marker rejected because showing near-misses the nearest-centre routing absorbed works against SPEC's aim-confidently wedge; held against [compile-and-view-panel] rather than stacking a second unverified change → 2026-09-01-key-press-feedback.md
- be3516e — [in-keyboard-voice-input] designed around press-and-hold with the control growing while held, on-device recognition only (no network fallback below API 31, so API 26–30 gets no mic), and the accent picker designed then dropped for reaching nobody whose accent comes from a first language; filed as `android-voice-input-and-accents.md` → 2026-09-01-in-keyboard-voice-input.md
- be3516e — [personal-voice-model] filed with on-phone training and enrolment audio destroyed after adaptation, after the user challenged this session's own 30-minute figure and research showed it describes full fine-tuning alone — LoRA beats it at low data, embeddings need seconds, one method needs no enrolment; destroying the audio recorded as killing the accumulate-over-sittings idea → 2026-09-01-personal-voice-model.md
- be3516e — [spec-coherence-readthrough] filed by /rescan as `[user]` work, from a suggestion made in passing and never written down: SPEC took seven changes in one session and nobody has read the result end to end; walkthrough asks for one uninterrupted read looking for machinery-in-product-truth and features outgrowing the perceptual wedge → 2026-09-01-spec-coherence-readthrough.md
- be3516e — Setup migration from document format 3 to 5: epoch 4 needed nothing (both cleared items already say what they change), but epoch 5's instruction to move the whole root `resources/` folder into `workshop/` was deliberately not followed — that folder also holds `key-layout.json` and `key-manifest.md`, which SPEC, the Gradle assets copy, `KeyLayout.kt`, the generator script and the README all name, so only `resources/research/` moved (by `git mv`, with a new index.md and the `.gitignore` payload-sample line repointed); CLAUDE.md's plugin-managed block replaced from the current template, gaining the sharper planning-only SPEC rule and a Visibility line filled from the project's own recorded answer; both queue section preambles quoted so the structure check stops reading them as headingless items; brevity output style enabled in a new `.claude/settings.local.json`; SPEC's stale "Project docs" section reported and left for the user; index rolled over into `index-2026-08.md` and `index-2026-07.md` → 2026-09-01-setup.md
