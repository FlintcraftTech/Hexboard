# LOG Index

One-line summaries of each session. Newest first. Each line names the session's full entry file in this folder.

- [HASH] — Setup migration to plugin 1.20.0-test12: only the recorded version needed bumping (format epoch, two-section queue and both `.gitignore` lines were already current), but the retired-terms check found batches, Deferred tests and `Parked:` still described in CLAUDE.md — and those descriptions proved to sit inside the plugin-managed block rather than in the user's own text, so the block was replaced wholesale from the current template instead of reported for the user to fix; the retired `Editor:` and `Working mode:` settings went with it → 2026-08-19-setup.md
- 67a7dd0 — The key config's `about` field, the generator script's hard-coded intro prose and the regenerated manifest all scoped to the three letter and symbol panels, stating the emoji panels are system-supplied and their absence correct rather than a gap; completes the carve-out SPEC took in the /plan session of the same day, kept as build work because the prose is duplicated in the script rather than read from `about` → 2026-08-14-emoji-panels-missing-from-config.md
- 67a7dd0 — Method renamed from Sovereign Implementer to Throughliner in the four live-instruction mentions: CLAUDE.md's opening line (inside the plugin-managed block, which a later /setup could rewrite) and its go-public paragraph, plus .gitignore's FAQ and payload-sample comments; LOG entries and queue prose deliberately left as written, so a grep still returning the old name is the correct result → 2026-08-14-throughliner-doc-drift.md
- 263eb26 — Go-public path cleared end to end: [github-org-case-drift] resolved in-session and deleted after the GitHub API confirmed the owner is `FlintcraftTech` with a lowercase `c` (two walkthrough occurrences corrected, git remote re-pointed off the rename redirect, and the capture's claim about the README found wrong); [repo-go-public] retagged `[user]` → build after `gh` proved to hold org admin, rewritten around one `gh repo edit --visibility public` command with two-sided verification and an explicit ask-before-running clause, marked `Runs alone`, lifted above the readiness line and moved ahead of the cleared `[user]` item because a run halts there and the validator test gates nothing; SPEC's manifest principle scoped to the letter and symbol panels after the user settled emoji as system-supplied, with [emoji-panels-missing-from-config] kept for the carve-out still needed in the config, the generator script (where the manifest prose is hard-coded, not read from `about`) and the regenerated manifest; [throughliner-doc-drift] narrowed to the rename in CLAUDE.md and .gitignore with LOG entries left as written; [compose-keyboard-renders-config] and [first-installable-build] filed, exposing that the queue had never held an item for building the keyboard itself, and named as blockers on the two held items → 2026-08-14-plan.md
- def1f5a — Project migrated from document format 1 to format 3 after the session-start check halted: `INBOX/` and `INBOX/archive/` created as this project's mailbox, `.gitignore` given `INBOX/` and `.throughliner/` lines, `.si-version` replaced by `.throughliner-version`, `.throughliner-format-epoch` written last so the halt cleared only after the conversions landed; QUEUE.md needed no conversion (already two-section), and three below-the-line items found lacking the now-required blocker reference, filed as [throughliner-doc-drift] rather than fixed → 2026-08-14-throughliner-migration.md
- def1f5a — README.md expanded from a one-sentence description to a full public-facing page above the untouched licence section: what Hexboard is with the perceptual claim stated as perceptual-and-testable and the functional version explicitly disowned, honest no-working-keyboard status, how to open the frozen browser prototype, and a pointer to LOG/ and QUEUE.md as a deliberately public planning record; layout screenshot rendered headlessly in Edge and cropped to the QWERTY panel with Pillow because the swipe carousel always bleeds neighbouring panels at both edges; method renamed to Throughliner in the text after fetching flintcraft.tech showed it never uses the old name → 2026-08-14-public-readme.md
- def1f5a — Root `.gitignore` created (the repo had none) listing FAQ/, the session-start payload sample and android/.idea/ with a reason comment on each, and FAQ/ untracked via `git rm --cached`; the payload-sample red flag carried through as cleared-by-design-out, with the record of why deleting it was self-defeating (the session-start hook rewrites it whenever absent); FAQ files remain readable in prior commits, a limitation accepted at planning → 2026-08-14-untrack-faq.md
- def1f5a — This repo's local git identity repointed at the GitHub noreply address so future commits carry it; the history rewrite ruled out and the reasoning recorded so it isn't re-proposed — the address is already public in two other repos, so a rewrite buys no protection while invalidating every hash reference in LOG/ and QUEUE.md; the address also sits in tracked file content at a42cd01, recorded as fact rather than an open question → 2026-08-14-git-noreply-email.md
- 24b811a — `hexboard-plan.md` read against SPEC, folded and deleted (five emoji panels and the claim's falsifiability into SPEC; its left-space relocation question out as capture [left-space-relocation]); [ignore-idea-folder] folded into [untrack-faq] so the root `.gitignore` writes three paths in one pass; SPEC's neighbour-derivation sentence corrected after establishing neighbour sets need the config plus the Kotlin-resident zag parity rule; [variant-editor] and [uniform-neighbours-predictive] sharpened and deferred; emoji panels found absent from the key config → 2026-08-07-plan.md
- e6742fc — Processed the go-public findings: planning record published by choice with FAQ untracked, noreply address set, README scoped, and the payload-sample red flag re-cleared by ignoring after deletion proved self-defeating → 2026-08-06-plan.md
- a42cd01 — Git history audited across all 6 commits before going public — machine paths, emails, personal detail and credentials; three passes clean (local.properties and .idea never committed), three findings captured: author email in all commit metadata, a candid personal line at QUEUE.md:88, and the internal planning record becoming public; red flag carried through as cleared, audit edited nothing → 2026-08-04-git-history-audit.md
- a42cd01 — LICENSE added with verbatim PolyForm Noncommercial 1.0.0 fetched from the PolyForm project and byte-checked, licensor named via a Required Notice line; README.md created minimal (title, one-line description, plain-English licence section stating source-available terms, permitted language forks, no commercial use, and that source-available is not open source) → 2026-08-04-add-licence.md
- a42cd01 — KeyLayoutValidationTest.kt added — six JVM checks over key-layout.json covering manifest rules 2 and 4 (cross-panel and within-panel duplicate outputs, row/col collisions, declared-bounds containment, every slot filled or noted-empty, distinct accent lists); Gson added as a test-only dependency; test unrunnable this session (Gradle loopback blocked) so all six checks were mirrored in Python and passed → 2026-08-04-key-config-validator.md
- a42cd01 — resources/key-layout.json created as the single source of truth for the key inventory; key-manifest.md converted to a generated artifact via scripts/generate-key-manifest.py with a --check drift mode; Gradle assets-copy task added so no second copy is checked in; hexboard17.html annotated frozen and planning/layout-preview.html annotated as an independent live fixture; the ten empty SYMBOLS slots each gained a recorded reason → 2026-08-04-layout-config-source.md
- 6e09dad — key-layout.json settled as the single source of truth for key data with key-manifest.md generated from it and geometry excluded from the config; android-key-audit split into key-config-validator (config soundness) and a render-and-emit check; licence-and-go-public split into add-licence (PolyForm Noncommercial), git-history-audit and [user] repo-go-public, red flag cleared by gating the flip on the audit; SPEC key-manifest principle and prototype-frozen line updated → 2026-08-04-plan.md
- dca16ac — Android Studio project created at android/ — Kotlin/Compose Empty Activity, package tech.flintcraft.hexboard, min SDK 26, Kotlin DSL; placed inside the existing repo so code and docs share one git history; Gradle sync verified → 2026-08-04-android-studio-setup.md
- dca16ac — resources/key-manifest.md created as the canonical key inventory — RARE/QWERTY/SYMBOLS panel maps with row/col, both long-press maps, the four inviolable rules verbatim from hexboard17.html, and a consumers list (hexboard17.html, planning/layout-preview.html) to reconcile on change; SPEC.md Principles gains a "canonical key manifest" principle pointing at it, worded around verify-before-ship rather than the prototype's audit_keys.js. → 2026-08-04-key-manifest-rules.md
- 718f9b7 — Second /plan of the day: reworded + queued key-manifest-rules as reference-doc + SPEC-principle build (top of Processed); deleted preview-harness, relocated fixture note to CLAUDE.md; filed layout-config-source / android-key-audit / variant-editor design-thread captures; recorded Working mode:local → plan-2026-07-29-2.md
- c92549a — Migrate QUEUE.md to two-section (Processed/Unprocessed) model, header re-copied from template; drop left-space relocation (asymmetric preferred by eye); add SPEC accessibility-node requirement + verify-a11y-ondevice [user]; set uppercase glyphs to 0.92× scaling in SPEC; build + capture planning/layout-preview.html harness → plan-2026-07-29.md
- 18ea3b9 — Adopt Hexboard into the Sovereign Implementer method; scaffolded SPEC/QUEUE/LOG/FAQ/CLAUDE from existing planning docs, filed 3 design questions as captures → setup-2026-07-02.md
