# [HASH] — [nested-wrap] opened by hand and stopped before its first move: Android Studio was running, which the item's own text never said to close

Date: 2026-09-18 09:35

This session ran across 2026-09-17 and 2026-09-18.

The session opened to run [nested-wrap] by hand — the conversion that puts the product into `hexboard/` inside the existing public repository and leaves the method's documents at the top in a private outer one. It got as far as the two things that have to be true before any file moves, and stopped at the third.

**What was done.** The item's entry and the wrap recipe in the installed setup procedure were read, and the split they describe was checked against the folder as it actually stands: twenty-three entries at the top level, seven of them staying with the product and sixteen coming back up, which is exactly the file-by-file split the item records. The scope file the method requires before a freeform session's first edit was written, naming the paths the conversion touches. The working tree was then made clean, which the item asks for by name: the only uncommitted change was the automatic hash backfill on the licence-out-of-readme record and its index line, and it was committed on its own so the wrap would land as a pure restructure rather than as a commit mixing a hash write with a repository-shaped move.

**Why it stopped.** Android Studio was running. `android/` is the largest folder moving down, and on Windows a move of a file another program holds open fails part-way — which is precisely the half-moved state the item's "by explicit name, never a glob" instruction exists to prevent. The item says Studio will need reopening at the new path *afterwards*; it says nothing about closing it *first*. So the stop was not a defect in the plan being executed but a gap in it, found by checking the machine before touching anything rather than by a failed move.

**What that leaves.** Nothing moved, nothing was rewritten, and [nested-wrap] stands exactly as it was written — first in the cleared region, still marked as work done by hand and as work that runs alone. The tree is clean at this close, so the next session inherits the state the item asks for.

**The amendment that was offered and not taken.** Putting the close-Studio step inside [nested-wrap]'s own text would place the warning where the session running it would read it. Amending a cleared item is the user's call; offered at this close, the capture was chosen over the amendment. The consequence is recorded rather than glossed: a session running the wrap before [studio-closed-before-wrap] is processed is warned by that queue entry or not at all.

**Queue changes:** [forward-advisory] replaced — the spent note naming the same item deleted and a fresh one filed in the freed slot, carrying why the item is named again and what the overlap scan found. [studio-closed-before-wrap] filed.

**Work processed:** none — no planning ran in this session.

**Routed to Captures:** [studio-closed-before-wrap], [forward-advisory]

**Advisory:** filed — forward-advisory

**Also in this chat:** the cycle on file, [emoji-data-refresh], was checked for due-ness at this close: its observable is a record under its own slug whose opening line says it records a completed turn, and the most recent is dated 2026-09-12 against an annual cadence, so nothing was filed. The scope file written for this session was offered for deletion at the close, being specific to a session id no later session can use.
