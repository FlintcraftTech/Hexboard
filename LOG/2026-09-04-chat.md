# 3838e52 — Planning session of 2026-09-03 to 2026-09-04, chat-level record: the install run's hold spent on nine items, six captures processed, three SPEC edits including a new principle the user raised, two method reports sent, and an hour lost to a permission prompt at an unattended machine

Recorded 2026-09-04 at 17:00. This session ran across 2026-09-03 and 2026-09-04.

The session opened on a queue where nothing was cleared to run and fourteen items sat below the line, ten of them held against [install-and-enable-on-pixel] — which had shipped and been removed the day before. The whole first stretch was spending that hold.

**The lift, and the one item held back.** Nine items lifted on a blocker that LOG recorded as built and verified: six builds held only to stop further edits stacking on files nothing had compiled, and three `[user]` steps that needed the keyboard on the phone and now had it. Each lifted item gained a line saying what cleared it, so the reason survives rather than the hold simply vanishing. [shift-behaviour] was the exception: its install hold was equally spent, but a capture filed the previous day found it assumed something the config could not do, so the hold was repointed rather than dropped, and released later the same session once that question was settled.

**Six captures were processed**, each with its own record: the shift-state labels, the wrong Settings path, the eleven-wide RARE panel, the navigation-bar overlap, the Drive build failure, and switch access. Two more were raised fresh by the user and processed in conversation — the soft key edge, and a principle about punctuation. Four captures were passed over silently, each waiting on an entry still open.

**Three SPEC edits, all written at their decision steps.** Letters rest in lowercase and rise with the shift state; a key is drawn as a soft-edged circle with no border, as wide as it is tappable; and a new principle, the user's, that nothing correctly spelled is ever changed on the user's behalf. That last one came from their annoyance at Gboard adding an apostrophe to every "its", and it is broader than the predictive engine — the distinction they agreed is between *offering* an alternative and *applying* one. It was written as its own principle rather than into the predictive-text one, so it also reaches dictation and tap-to-replace, and notes were written onto all three items it binds. It produced no queue item: nothing in any file changes, so it is a constraint rather than work.

**[declare-savedstate-viewmodel-deps] was amended on the user's direction**, its "a lookup the build does first" replaced with the answer. Half needed no lookup — the catalogue already carries a lifecycle version the new artifact can ride — and the other half was one read: `androidx.savedstate:savedstate` at 1.5.0, published 2026-05-19, with a note that the version can move.

**Three captures had their `Blocked by:` lines cleaned**, naming a blocker that no longer exists, each gaining a clause saying what released it. Two candidates were considered and dropped: a `CLAUDE.md` rule about visual constants, because the method fix is expected to cover it, and a SPEC read-through, because one passed two days ago and would compete with four `[user]` items already waiting.

**Also in this chat:**

- **Two reports were sent to the plugin's own project**, both approved verbatim before going. The first proposes that `/setup` warn when a project folder sits inside a cloud-sync tree, carrying the user's own finding that mirroring beats streaming whatever the provider, and stating three limits including that mirroring reduces the collision rather than removing it. The second reports that the buildability check forbids scheduling a design decision into a build without saying where a decision ends and a tunable constant begins — raised by the user from noticing this session was reading it more strictly than they were used to.
- **The plugin updated mid-session**, changing the governing rules under the work. Both rule documents were re-read and the delta applied: one new rule caught an assertion this session had just written into two walkthroughs about what a Settings search would return, and the wording was corrected to a guess with a fallback. Recorded on that item's own entry.
- **An hour was lost to a permission prompt.** A browser call made to render a design comparison sat on an unanswered "allow once" dialog at a machine the user was away from — they were driving remotely and had to ask someone to go and clear it. Twice, before all tool calls were stopped. The comparison never reached them, and the design question it existed to settle was answered by their direction instead. A memory note was written so later sessions prefer sending a file over rendering one when the user is remote.
- **A correction the user made to this session's own reasoning:** that small visual adjustments belong inside a build. It changed how [soft-key-edge] was written and produced the second method report.

**Queue changes:** nine items lifted above the readiness line; [shift-behaviour] repointed then released; five new items created ([build-output-off-drive], [board-clear-of-navigation-bar], [soft-key-edge], [rare-row2-unindent], [verify-switch-access-ondevice] cleared, [settings-steps-name-a-search] held); six captures deleted; the spent forward advisory cleared at the opening; Unprocessed reordered by unblock potential at the start of processing. Processed ends the session with fifteen items cleared to run and five held.

**Work processed:** kept — [soft-key-edge], [verify-switch-access-ondevice]; deleted — [uppercase-labels-vs-shift-state], [install-walkthrough-settings-path], [rare-panel-eleven-wide], [board-under-navigation-bar], [drive-sync-locks-gradle-build], [forward-advisory].

**Wind-down re-scan:** covered by the rescan just run.

**Advisory:** not needed — no single item is the right place to start, and the queue's own order carries the priority.
