# e6742fc — Processed the go-public findings: planning record published by choice with FAQ untracked, noreply address set, README scoped, and the payload-sample red flag re-cleared by ignoring after deletion proved self-defeating

This session worked through the findings of the [git-history-audit] pass, which together form the gate [repo-go-public] waits behind. Four of the five decisions were about what publishing this repo actually exposes.

**The red flag, and how it cleared — including a wrong turn worth recording.** `resources/research/session-start-payload-sample.json` holds this machine's account paths, a session ID and a local transcript path. The session first cleared the flag by deleting the file, reasoning that it was research about the Sovereign Implementer method rather than Hexboard content and so had no home here. That was wrong, and it was caught at this close: `git status` showed the file back with a *different* session ID and a later timestamp. The plugin's own session-start hook writes it whenever it is absent, so deleting it does not remove the risk — it guarantees a fresh copy, carrying the next session's paths. The flag was re-cleared correctly by ignoring rather than deleting: [untrack-faq] now also lists the file in the root `.gitignore` it creates, so no session can stage it, and that item already gates [repo-go-public] so there is no window where the repo is public and the file is not ignored. The hook's behaviour is a method issue rather than a Hexboard one and was deliberately not filed in this queue.

**The email finding shrank once checked.** [git-history-email] proposed weighing a history rewrite against leaving the address exposed. Checking GitHub settled it: `FlintCraftTech/sovereign-implementer` and `FlintCraftTech/Taskflowapp` are both public and every commit in each already carries the same personal address. A rewrite would change every commit hash, force a rewrite of the pushed remote, and invalidate every hash reference in `LOG/` and `QUEUE.md` — real cost to hide something already visible. Ruled out, with the reasoning recorded in [git-noreply-email] so it isn't re-proposed. What survives is the cheap half: point this repo's git identity at a GitHub noreply address. A global git identity was also set this session at the user's request, after finding there was none at all; it covers only newly-created repos, because every existing repo carries a local identity that overrides it.

**Claude leaked the address mid-session and the user caught it.** While drafting [git-noreply-email], Claude wrote the literal address into QUEUE.md — into the very item arguing against publishing it. Scrubbed immediately and the working tree verified clean. The durable fix is a standing rule now in CLAUDE.md: never write personal contact details into a tracked file, including in text *about* those details. A pre-existing copy remains in file content at `a42cd01`, recorded as an accepted limitation under the same rewrite reasoning.

**Publishing the planning record is now a decision rather than a default.** [planning-record-public] resolved to publish: the user already ships `sovereign-implementer` publicly, so Hexboard's `LOG/` and `QUEUE.md` are a worked example of their own public method, and that demonstration value is the point. The ongoing cost was named and accepted — every future planning session writes to a document strangers read, and a queue read by strangers is a queue thought in less freely. `FAQ/` is the one exception: Claude initially misread it as stray duplication, and the user corrected that — it is installed per-project so both of them can consult the method mid-session. It leaves tracking but stays on disk. CLAUDE.md now carries both the decision and the writing standard it implies: candid about the work, never about the person.

**Two items dissolved on inspection rather than being built.** [queue-candid-line] was resolved in-session — the line "Alex has flagged this as beyond him" inside [variant-editor] was reworded to frame the fork-sync question as a property of the question, and the item deleted since nothing remained. [licence-legal-read] was consciously declined, because its premise was wrong: the licence choice is not irreversible. The copyright holder can relicense future versions at any time; only the grant on already-published copies is permanent, and with no public repo there are none. The trigger the item missed is recorded instead — outside contributors, whose copyright in their own contributions is what genuinely makes a licence hard to change later.

**A user-only item was tested before being accepted as user-only.** [run-key-config-validator] could have been an ordering concern rather than user work, so Claude tried to run it: four routes, all failing on Gradle's loopback connection to its own daemon, including with the sandbox disabled at the user's explicit permission. The block sits below anything Claude can reach and will not clear by waiting, so it is a genuine capability gap and the `[user]` tag is correct. All four attempts are recorded in the item so no future session repeats them.

**A capture was filed, sharpened, mis-kept, and corrected.** The user's observation that every interior key has exactly six equidistant neighbours turned into a real design argument: their standing complaint is that a wrong *first* letter breaks autocomplete far worse than a wrong later one, because prefix lookup treats the first letter as certain. The neighbour set dissolves that — the engine can search forwards from all seven candidates and rank by geometry times word frequency, rather than searching backwards to compensate. SPEC's predictive-text principle was rewritten accordingly and now also states that the engine is built rather than borrowed, because no general-purpose library knows the key geometry. Claude then kept the item into Processed as buildable work; the user caught it, correctly — design is not a build, and the keep test is being able to describe what would change. It was returned to Unprocessed with the sharpened reasoning preserved.

**Queue changes:** [untrack-faq] rewritten to create the root `.gitignore` covering both `FAQ/` and the payload sample, and given the red-flag marker at `State: cleared`; [repo-go-public]'s lift-condition rewritten to name all five prerequisites and to spell out the three history limitations consciously accepted; [variant-editor] skipped to the bottom of Unprocessed; the consumed forward-recommendation advisory cleared, and a stale one from the session before it deleted; SPEC.md's predictive-text principle rewritten; CLAUDE.md given the public-audience writing rule; four items now sit above the cleared-to-run line. Two captures filed at the close's wind-down re-scan: [stale-plan-doc] and [ignore-idea-folder]. A forward-recommendation advisory, [advisory-3], was also filed after the commit, pointing the next session at the three cleared builds and at processing [ignore-idea-folder] before them.

**Work processed:** kept — [git-noreply-email], [untrack-faq], [public-readme], [run-key-config-validator]. Deleted — [session-payload-sample] (resolved, then its flag re-sited on [untrack-faq]), [queue-candid-line] (resolved in-session), [planning-record-public] (decided, consequences filed), [licence-legal-read] (declined), and the stale advisory. Filed and sharpened but left unprocessed — [uniform-neighbours-predictive]. Skipped — [variant-editor].
