# [HASH] — [switcher-subtype-label] one subtype labelled "Hexboard", and the never-asked question of whether to declare one per layout answered no

Session of 2026-09-12, 12:54.

Android's Change Keyboard card shows a subtype label large with the app's name small beneath, and Hexboard's label was "English (QWERTY)" — so the user, told to switch to Hexboard on 2026-09-09, reported it was not on the card. It was, in third place.

The label also became false when the layout picker shipped on 2026-09-05: one install types seven languages, so a subtype announcing English is a claim the app no longer honours.

**The question underneath had never been asked, and it is answered no.** Subtypes are how an input method exposes languages to the system, so seven of them would put a second language chooser in Android's own switcher. SPEC deliberately puts that choice in the app's settings, and two choosers for one setting is how they come to disagree — with the switcher's being the one nobody maintains.

So: one subtype, labelled "Hexboard". The row then carries the word a person is scanning for. It repeats the app name on the second line, which is the price of being findable and costs nothing else, there being no variant for that large line to distinguish.

One limit recorded rather than glossed: how the card renders with the same word on both lines cannot be seen from this machine, so the item names it as a check to make rather than a fact anyone has established. [judge-drawing-values-on-phone] is the sitting that makes it.

**Queue changes:** [switcher-subtype-label] designed out and cleared to run; SPEC gained the sentence saying Hexboard announces itself as one keyboard rather than one per language.

**Work processed:** kept — [switcher-subtype-label].
