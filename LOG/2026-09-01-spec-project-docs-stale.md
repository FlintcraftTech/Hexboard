# be3516e — SPEC's scaffold-written "Project docs" section cut entirely rather than replaced by a pointer

The section listed SPEC, QUEUE and LOG and said what each was for. Three things were wrong with it, one more than the item had noticed: it was method machinery inside a document whose job is product truth; its wording was retired, calling QUEUE.md "work batches"; and it said "three project docs" when the project has six.

Nothing was lost by removing it. CLAUDE.md's plugin-managed block carries the same description and `/setup` keeps it current, and README.md already points a public reader at LOG/ and QUEUE.md as a deliberately public planning record — both more current than SPEC would be. A one-line pointer was the alternative and lost for the same reason the section did: it would still be method machinery in a document describing a keyboard.

Cut in-session rather than queued, under SPEC's own rule that a section describing something the project no longer has is wrong rather than merely surplus, and is corrected when noticed. The item was then deleted as done.

**Queue changes:** [spec-project-docs-stale] deleted from Unprocessed; SPEC.md lost its "Project docs" section.

**Work processed:** deleted after completion — [spec-project-docs-stale].
