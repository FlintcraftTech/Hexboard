# 5f9d97e — [predictive-saved-words] split out as a capture, carrying the red flag on a consent footing rather than a designed-out one

Split out of [uniform-neighbours-predictive] on 2026-09-05, being the one part of the engine that stores anything. That item's record for the same date carries the wider reasoning.

The thing a later session must not misread, restated here because this is where someone would look for it: the flag is cleared **by the user's informed consent to a smaller risk**, not by design-out. The 2026-08-20 decision was a fixed dictionary recording nothing; on 2026-09-01 he was told plainly that saving words means the device holds a file of words he typed, and chose it anyway. Three constraints came with that consent and bind the build — the list grows only by a deliberate save, it never leaves the device, and he can read and delete it.

It stays a capture because nobody has chosen how a word gets saved or where the list is read, so what a build changes cannot be stated.
