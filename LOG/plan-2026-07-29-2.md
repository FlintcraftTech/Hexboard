# 718f9b7 — Process key-manifest & preview-harness captures; queue key-manifest-rules build, relocate preview-fixture note to CLAUDE.md; file config/audit/editor design-thread captures; record Working mode:local

Second planning session of the day. Went through the two unprocessed captures and opened a design thread. **key-manifest-rules** was kept as a build, but reshaped: rather than dumping the prototype's key map into SPEC, it writes a dedicated `resources/key-manifest.md` (verbatim map + four inviolable rules) and adds a short SPEC principle pointing at it — keeping SPEC readable while making the manifest canonical. The user flagged that the preview harness also hand-copies key data, so the item now also makes the manifest doc declare itself the source of truth and name its copies (hexboard17.html, the preview harness), giving rule 3 ("no silent changes") a concrete target. **preview-harness** wasn't buildable work — the tool already exists — so its substance moved to CLAUDE.md Project rules as a standing-fixture note and the queue item was dropped. A question about a contributor-facing layout editor for language/key-set variants opened a design thread: it presumes a single machine-readable layout config as source of truth, so that prerequisite (**layout-config-source**) was split out and ordered ahead of its consumers. Working mode was recorded as `local` in CLAUDE.md.

**Queue changes:**
- Kept **key-manifest-rules** to Processed (top, above android-studio-setup), reworded to the reference-doc + SPEC-principle split with a drift cross-reference note.
- Deleted **preview-harness**; relocated its substance to CLAUDE.md Project rules.
- Filed three captures to Unprocessed: **layout-config-source**, **android-key-audit**, **variant-editor** (ordered by unblock-potential).
- Recorded `Working mode: local` in CLAUDE.md.

**Work processed:** kept — key-manifest-rules; deleted — preview-harness. (android-key-audit, layout-config-source, variant-editor filed, not yet processed.)
