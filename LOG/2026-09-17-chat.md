# [HASH] — Planning session of 2026-09-17, chat-level record: a queue emptied to nothing presentable, a setup top-up run inside it, and the repository shape reversed

Recorded 2026-09-17 13:59. Thirteen entries accompany this one, one per item processed; this carries what belongs to no item.

The session opened with 19 items cleared, 11 captures and a throughput floor of zero — nothing was held on work sitting in Unprocessed, so no item had to be processed to release another. It ended with 22 cleared, 7 captures, and every one of those seven passed over: five waiting on other entries and two on dates. The queue came to rest by passing everything over rather than by being fully processed, which is stated because the two look identical from the counts alone.

**Also in this chat:**

**The mailbox held two messages that were this project's own outbound reports**, kept as local copies rather than anything incoming — a defect report on `/done`'s close marker and on `inbox_send.py`'s undocumented address-book format, and a correction narrowing the first report's cause from an app restart to the plugin being replaced underneath a live session. Both are the plugin's business and already in the sent register, so neither produced work here. One thing in them did: the `.gitignore` observation that became [temp-not-gitignored]. Both files were archived.

**A data point on that same defect, worth recording because it bears on a report already sent.** This session's `/done` wrote its close marker to the scratchpad without trouble, where the reported failure was a refusal of every nameable scratchpad path. This session started and stayed on 1.23.0, which is consistent with the corrected cause — a version change under a live session — rather than with the restart the first report blamed. It is one clean observation, not a test of the hypothesis.

**Four unsourced time words were stopped and corrected** — "today", "just now", "this morning" and one more — each caught by the safety check rather than by the session, and each rewritten to name its source or drop the word. The pattern in every case was the same: a true statement about when something happened, written with nothing behind it.

**The owner chose to run a setup top-up inside this planning session** after being told the reason not to: six files were already modified and uncommitted, so the migration and the planning work would land in one commit. They did. The per-item records are what separate them for a later reader.

**Claude's recommendations were rejected twice, both times on the owner's own grounds**, and both reversals are recorded on their items rather than only here — the long-word completion slot, removed after he asked what length had to do with it; and the flat repository shape, kept public by a decision of 2026-08-06 and now reversed by [nested-wrap].

**The memory check found every run the files record still in view** — the queue diff proving a planning run, and the setup run, both with their invocations and opening lines present in the conversation.

Mail, issues, replies, cycles and the goal check: the issue channel was quiet in all three limbs, no cycle was due, and the emoji-data-refresh cycle's annual cadence has not come round since its 2026-09-04 reading.

**Advisory:** filed — [forward-advisory]
