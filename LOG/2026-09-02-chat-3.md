# 37384e4 — Planning session of 2026-09-02, third of the day: the install walkthrough lifted out of a deadlock, a row above the keys split out of voice input, and emoji panels finally given an item

This session ran from a queue with nothing cleared to run and three `[user]` items held below the line, all waiting on work that had shipped that morning and never compiled.

**The lift, which the ordinary rule would have refused.** [install-and-enable-on-pixel] was held against [first-installable-build], which shipped that day recorded as unconfirmed. A built-but-unverified blocker normally keeps an item held — but this walkthrough *is* the verification, its step 4 being the Android Studio run that compiles the service, so the item and its blocker were waiting on each other and the queue had no runnable work at all. Lifted, with that reasoning written onto the item. done-plan.md names this exact case as the exception, which is what the lift was checked against. Two further held items had their holds repointed off shipped blockers onto what genuinely holds them.

**A row above the keys became an item of its own, reversing a recommendation made an hour earlier in the same session.** Designed first as part of [in-keyboard-voice-input] to hold the microphone, with Claude recommending against splitting it out because one feature needed it; split out as [suggestion-strip] once [persistent-clipboard]'s screen was settled as reachable from its left end, because the two features are held by different things and the clipboard would otherwise have waited behind a dictation test. Three of the session's items now depend on it and a fourth clamps its accent row into it.

**Emoji panels were found missing entirely.** A grep during the clipboard discussion found no vertical swipe and no emoji panel in the Kotlin, against a SPEC promise of five. The research that followed established that Android sells its emoji list only bundled with its own picker interface, so SPEC's five-panel promise and its "system-supplied content" promise could not both stand; the user chose Unicode's own published list, which keeps Hexboard's panels, and SPEC's wording was sharpened to match. Filed as `android-emoji-sources.md`.

Ten items were processed against a throughput floor of two, and four new items were filed. Everything settled today sits behind the install walkthrough, directly or one hop away.

**Queue changes:** [install-and-enable-on-pixel] lifted above the readiness line; [suggestion-strip], [row-tint], [physical-keyboard-handover] and [emoji-panels] filed and processed into Processed below the line; [in-keyboard-voice-input], [persistent-clipboard], [ship-all-layout-configs], [shift-behaviour], [panel-key-size-consistency], [declare-savedstate-viewmodel-deps], [accent-row-top-row] and [russian-layout-check] moved from Unprocessed into Processed below the line; [layout-switching] kept in Unprocessed on a repointed hold; [tap-word-alternatives] filed as a capture; [uniform-neighbours-predictive] and [speech-output-correction] amended in place; the spent forward advisory cleared at the opening. Processed reordered to batch the `[user]` stops at the end of the held region, except [recogniser-gap-comparison], which stays ahead of the item that depends on it. SPEC gained seven sentences across the voice, clipboard, layout and manifest sections.

**Work processed:** kept — [in-keyboard-voice-input], [russian-layout-check], [persistent-clipboard], [ship-all-layout-configs], [shift-behaviour], [panel-key-size-consistency], [declare-savedstate-viewmodel-deps], [accent-row-top-row], [suggestion-strip], [row-tint], [physical-keyboard-handover], [emoji-panels]. Held in Unprocessed — [layout-switching]. Deleted — none.

**Advisory:** filed — [install-and-enable-on-pixel]

**Also in this chat:**

Five corrections, four of them Claude's errors caught in the moment. Claude described per-panel key sizing as making the board "unstable", which overstated a resize into a fault, and the user asked where that had been said — the correction is recorded on [panel-key-size-consistency] because their first answer was given against the overstated framing. Claude stated Android's 48dp minimum touch target from memory, flagged it as recalled rather than read, and looked it up on the user's word. Claude told the user `planning/layout-preview.html` offers only the Russian layout; it carries four entries, and the nit built on that was wrong — nothing had been written into any item on the strength of it. Claude called the user's Gboard homophone observation evidence for one of two explanations, and they pointed out it is ambiguous between them, which is why that finding is worded as *not found* rather than *established absent*. And an ask that restated two roads instead of asking about one was rewritten as a single question after the user said the ask was unclear — they had already decided, and could not tell which option was being put.

The user's own reasoning carried three decisions this session that Claude had not reached: that a wrong keystroke is cheaper to correct than a missing one, which is now the standing argument for the board's full-coverage routing; that rows rather than columns should be tinted, because reading runs left to right; and that doing the emoji work early buys time to resolve it rather than deferring the cost.
