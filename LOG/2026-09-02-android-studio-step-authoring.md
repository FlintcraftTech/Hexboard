# 6f66348 — [android-studio-step-authoring] kept as a one-sentence CLAUDE.md rule, and the instance reported to the plugin's own project by mail

Planning record for [android-studio-step-authoring], 2026-09-02.

From the morning's drive: a step said to press Shift twice to open Android Studio's search box; nothing happened and there was nothing on screen to report. A click path down the Project pane worked first time. The rule: a walkthrough step for a GUI app names something visible to click or a menu path, and a shortcut may ride alongside as an aside but is never the instruction — the aside form keeps shortcuts available once known. Its home is `CLAUDE.md`, which governs how Claude works on this project; a planning session may not edit that file, so it is a one-sentence build, placed beside [claude-md-phase-ran] which edits the same file. Rule-gate disposition written on the item: one rule added, evicting nothing.

The user asked for the instance to go to the plugin's own project as well, since the method's walkthrough rule already asks each step to name the thing to click and this sharpens it. The recipient mailbox was confirmed present and gitignored, the exact text was shown, and it was sent on the user's yes; the register line is in `INBOX/sent.md`.
