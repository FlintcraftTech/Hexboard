# PLACEHOLDER — Planning session of 2026-09-02, second of the day: six held items lifted after the first real run, the Russian layout redesigned around an open-source source, and the panel's four gesture items put in build order

Chat-level record for the planning session that opened at 14:33 on 2026-09-02 and closed at 15:36, after the /next run of the same morning had closed. Each item processed has its own entry of the same date; this records what belongs to no single item.

**The session opened on an unclosed run from another chat.** The morning's /next run had built five items and driven three `[user]` walkthroughs but never closed, so its records and code changes sat uncommitted and three finished walkthroughs still sat in the cleared region. The session named that before anything else and asked whether the other chat was still open; the user closed it there, and this session restarted against the committed queue. The plugin was updated between the two openings (1.21.1-test5 to test6), so the rules were re-read and the differences noted.

**Six held items were lifted above the line once the run's records were on disk**, on the basis that their blockers were now built *and* verified: the compile on the Pixel 6 verified the rendering item, the SPEC principles rework was confirmed by its own greps. Each lifted item gained a line saying what cleared it and whether anything had verified its own premise since it was written. [left-space-relocation] was the one held item whose blocker resolved *against* it — the user kept the 4-and-6 space bars — and it was deleted on the user's agreement.

**The Russian layout was redesigned twice in the session, and the second time settled the method for every language.** The slot-count halt from the morning was confirmed against the config (26 letter positions, not 30). Claude offered three routes; the user rejected all of them as improvisation and supplied a Gboard screenshot, which showed the standard Russian keyboard is eleven columns wide. That reversed the morning's rejection of widening: there is no ten-column standard to copy. The user then proposed screenshotting Gboard for every language, tried it, and withdrew it the same hour when Gboard offered five Russian layouts and the reading was work the user's own visual processing made costly. Claude proposed open-source layout data instead, searched with the user present, and found FlorisBoard's Apache-2.0 files carry both the rows and the hidden letters — Ё under Е, Ъ under Ь — so the arrangement is transcribed from a file and nobody reads a phone by eye. Filed as `workshop/resources/research/open-source-layout-sources.md`; the earlier Cyrillic finding is marked superseded at its top. The user's one condition, in their words: "as long as we don't have missing keys, that's the main thing."

**One design principle came from the user and now binds every hold on the keyboard**: timing follows the phone's own touch-and-hold accessibility setting, because the user sets phones up for people slower than themselves. Claude's first proposal, AOSP's fixed 400 ms / 50 ms, lost to it. Written into SPEC and into the repeat item.

**The four items editing `KeyboardPanel.kt` were put in a single build order** — feedback, repeat, accent popup, swipe — each introducing something the next relies on, with the swipe last because it must cancel all three. The accent popup was found by the rescan: the config declares eighteen long-press lists, SPEC promises the feature, and nothing in the Kotlin implemented it or was queued to.

**SPEC gained five sentences this session**, each written at its decision step with the user present: the layout source and the widened-rows rule; hold timing following the phone's setting; the wide-screen split whenever width exceeds height; and one tightening at the close (no *letter* is hidden or dropped, since the Russian layout does drop cursor-right, which is a function key).

**Also in this chat:** the two `[user]` items and one capture were rewritten from the morning's drive — the install walkthrough gained the run-configuration and sync-before-Run checks and lost its assumed pairing, and one capture was processed by rewriting that walkthrough directly rather than queuing it as a build. One method report was sent by mail to the plugin's own project, on the user's suggestion, about a keyboard-shortcut step that failed silently; the send is in `INBOX/sent.md`. The session-start note about an unfilled hash placeholder in the index was checked and nothing was found.

**Queue changes:** six items lifted above the cleared-to-run line; nine captures processed (six kept into Processed, three deleted after relocating their content); one capture held in Unprocessed with a blocker; two held items' walkthroughs and prose updated; a build order written across four `KeyboardPanel.kt` items.

**Work processed:** kept — [backspace-key-repeat], [assets-srcdir-deprecation], [android-studio-step-authoring], [claude-md-phase-ran], [long-press-accent-popup], and [language-starter-layouts] rewritten and cleared; held in Unprocessed — [split-layout-wide-screens]; deleted — [left-space-relocation], [qwerty-letter-slot-count] (relocated into the Russian item), [install-walkthrough-refresh] (relocated into the install walkthrough).

**Routed to Captures:** none beyond those processed in-session.

**Wind-down re-scan:** covered by the rescan just run; the one item it surfaced was processed in the same session.

Advisory: not needed — the cleared region opens on [first-installable-build] with nothing in Unprocessed bearing on it, so the ordinary top-down run is the recommendation and there is nothing specific to advise.

This record is checked against the scrub checklist and the credential scan, and against nothing more.
