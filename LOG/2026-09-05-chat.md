# [HASH] — Planning session of 2026-09-04 to 2026-09-05, chat-level record: sixteen items given a disposition against a throughput floor of zero, eight SPEC sentences, the project's first cycle, three research findings, one method report sent and a second declined after checking

This session ran across 2026-09-04 and 2026-09-05.

It opened with three items cleared to run, all of them `[user]` checks the user had deferred the day before for the same reason — that run's build is not on the phone — so a build run would have produced nothing. The dependency facts gave a throughput floor of zero: nothing in Unprocessed was blocking anything, so everything done past that was worth doing rather than required. It ended with fifteen items cleared and nothing left in Unprocessed that can be offered.

**One pattern ran through the whole session and is the thing most worth carrying forward.** Three separate decisions turned out to rest on premises the project had itself removed later, with nobody returning to the decision: SPEC's predictive-text deferral, whose condition was met by the install of 2026-09-03; the layout-confirmation requirement, whose cost the record had already identified as the bottleneck; and the editor page kept as prior art for an item deleted three days ago. Each was caught by looking, not by anything systematic. A method report went to the plugin's own project naming the asymmetry behind it — a work item carries a rests-on line dating the facts it assumes, and a product-truth sentence carries nothing equivalent, so a sentence resting on a condition met or removed elsewhere goes on reading true. The report carried the one recorded instance, a same-day near-miss, and one suspected instance that inspection cleared, with the over-reporting risk stated.

**A second report was drafted in the mind and declined after checking**, which is recorded on [walkthrough-steps-quote-screen-text]: the method already has the rule that would have caught the "availability line" failure, and already says that rule has no enforcement.

**Corrections made this session.** An assertion that two SPEC sentences had gone stale unnoticed was narrowed to one after checking — the second was caught within the same session, and a third was a false alarm, having been written as part of the very change it was suspected of predating. A size answer about languages was incomplete and was corrected on the user's challenge: 17 KB is the layout, not the language, and the dictionary is a megabyte or two with its own licence hunt. The rescan's first framing of the editor page as an oversight was corrected to what the record actually shows. And a figure written into `KeyGeometry.kt` yesterday — "about 1.18, where neighbouring fades just meet" — was found wrong by arithmetic; they meet at 1.045.

**The user's own contributions, which changed designs rather than approving them.** His diagnosis that the soft edge's gradient had been built inside the old circle rather than around it, which turned two complaints into one cause. His judgement that a native reader for every layout is unrealistic, with easy bug reporting in its place, and his aim of catching complaints before the Play Store. His question about whether a language brings its whole dictionary, which produced the two-speed plan and the asset-delivery route. His correction that the phrase prompter was for training the speech model rather than for testing recognisers. And his question about whether contacts could be kept out of dictation biasing simply by not enabling it.

**Also in this chat:** the clock passed midnight during the session, so items written before it carry 2026-09-04 and everything after carries 2026-09-05. The queue's stale holds were tidied — [in-keyboard-voice-input], [persistent-clipboard] and [accent-row-top-row] all named blockers that had left the queue, and now name the install that would actually prove them. At the close the `[user]` install item moved to the end of the cleared region, so a run builds all fourteen build items and then hands over one sitting that verifies the lot.

**Queue changes:** sixteen entries given a disposition; twelve new items created; three items deleted ([russian-layout-check], [label-size-after-soft-edge] merged, [language-list-choice] and [emoji-data-refresh] relocated into their successors); the cleared region grew from three items to fifteen; `CYCLES.md` created with the project's first cycle; eight SPEC sentences written or rewritten, two of them caught by the close's spec-sync gate; three research findings filed and two existing ones corrected.

**Work processed:** kept — [verify-this-runs-build-on-device], [soft-edge-fraction-values], [uniform-neighbours-predictive], [predictive-neighbour-table], [predictive-dictionary-bundle], [tap-word-alternatives], [speech-output-correction], [layout-error-report], [layout-switching], [first-batch-layouts], [split-layout-wide-screens], [landscape-board-height], [rsvp-dictation-prompter], [prompter-elicits-natural-speech], [status-lines-after-install], [symbols-panel-empty-slots], [emoji-panel-reach], [emoji-parse-guard], [walkthrough-steps-quote-screen-text], [dead-key-border-colour], [remove-superseded-editor-page]. Deleted — [russian-layout-check], [label-size-after-soft-edge], [language-list-choice], [emoji-data-refresh].

**Routed to Captures:** [predictive-saved-words], [dictation-biasing-saved-words], [feedback-funnel-before-store], [dictionary-asset-packs], [competing-layout-standards], [enrolment-prompter], [russian-panel-gaps], [emoji-data-refresh].

**Cycles:** one cycle exists, `emoji-data-refresh`, authored this session. It is due — its observable is a record whose opening line says it records a completed turn, and none exists yet, so it is due by construction on its first check — and a capture was filed for that first turn. The turn will probably find nothing to change, since Unicode's highest published version on 2026-09-04 was the one already bundled; running it anyway is what gives the observable something to read.

**Wind-down re-scan:** stood down — covered by the rescan run immediately before this close. What happened between the two is the close's own work: the spec-sync gate caught two sentences, and both are recorded on the items that made them wrong, [split-layout-wide-screens] and [speech-output-correction].

**Session-file cleanup:** nothing session-created looks throwaway. The working files this session wrote were all queue-item bodies and research findings that landed in the repository, and the drafting copies live in the session scratchpad outside it.

Advisory: not needed — nothing in Unprocessed is offerable, every remaining capture being held behind an entry that is still open, so the next planning session has nothing to process until a build releases something.
