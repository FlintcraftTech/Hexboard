# [HASH] — The layout editor held against the new variant-identity item rather than skipped again, and kept in Unprocessed deliberately

The reasoning behind the split is in the entry for [variant-schema] and is not restated here. What this entry records is where [variant-editor] landed and why it did not go further.

It gained `Blocked by: [variant-schema]`, which on a capture means it stops being offered until that item resolves and then returns by itself. Its prose now records that half the 2026-08-20 reasoning did not survive re-reading — the schema was said to follow from a build, and only the runtime half does.

It was deliberately not filed below the cleared-to-run line. Below the line means designed and buildable but held, and this is not buildable even with the schema in hand: what the editor actually is — a page in `planning/`, a rework of the existing `hexboard-editor.html`, something else — and what it writes are still undescribed. Claiming a buildability the item does not have would have been the easy error here.

The item's own note that nothing is waiting on the user was kept and remains true.

**Queue changes:** [variant-editor] gained a `Blocked by:` line naming [variant-schema]; stays in Unprocessed.

**Work processed:** kept in Unprocessed, held — [variant-editor]. Reasoning in the entry for [variant-schema].
